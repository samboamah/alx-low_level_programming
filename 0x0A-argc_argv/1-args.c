#include <stdio.h>

/**
 * main - prints number of arguments passed in.
 * @argc: number of command line arguments.
 * Return: 0 - success.
 */
int main(int argc)
{
	printf("%d\n", argc - 1);
	return (0);
}
